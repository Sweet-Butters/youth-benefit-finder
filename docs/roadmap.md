# 기획 로드맵

> 작성일: 2026-09-25. 원칙은 **검증 → 사람 손으로 연결 → 필요한 것만 자동화**다.
> 문제는 아직 가설이므로, 코드보다 인터뷰 결과가 먼저 방향을 정한다. → [검증 계획](validation-plan.md)

## 1. 단계와 판단 지점

인터뷰 결과에 따라 갈라지는 지점을 미리 정해 둔다.

```mermaid
flowchart TD
    A["0단계: 꿈드림 봉사 + 인터뷰<br/>2026.10~12"] --> G1{"H1: 정보를 못 찾아서<br/>혜택을 놓치나?"}

    G1 -- 예 --> B["1단계: 혜택 찾기 MVP<br/>수작업으로 모은 혜택 30~50개"]
    G1 -- "아니오: 동기·막막함이 원인" --> M["매칭 중심으로 전환<br/>2단계로 바로 이동"]

    B --> G2{"H2: 센터 선생님이<br/>다 챙겨 주지 못하나?"}
    G2 -- 예 --> B2["청소년이 직접 쓰는 도구"]
    G2 -- "아니오" --> B3["선생님용 도구 또는<br/>센터 밖 청소년 대상"]

    B2 --> G3{"H3: 서울 원스톱서비스가<br/>틈을 메우고 있나?"}
    B3 --> G3
    G3 -- "서울은 틈이 작음" --> R1["비서울 지역부터"]
    G3 -- "틈이 있음" --> R2["전국 대상"]

    R1 --> C["2단계: 사람이 직접 하는 매칭<br/>선생님을 통해 멘토 연결"]
    R2 --> C
    M --> C

    C --> G4{"H4: 연결되면<br/>실제로 행동하나?"}
    G4 -- 예 --> D["3단계: 매칭 기능 개발<br/>로그인·DB 도입"]
    G4 -- "아니오" --> X["매칭과 수익 모델 재검토"]

    D --> G5{"H5: 기업이<br/>돈을 낼까?"}
    G5 -- 예 --> E["4단계: 기업 사회공헌<br/>+ 임팩트 리포트"]
    G5 -- "아니오" --> F["B2G, 기관 구독 우선"]
```

![단계와 판단 지점](images/roadmap-decisions.svg)

## 2. 일정

```mermaid
gantt
    title youth-benefit-finder 로드맵
    dateFormat YYYY-MM-DD
    axisFormat %y.%m

    section 0. 검증
    꿈드림 봉사 시작, 신뢰 쌓기   :v1, 2026-10-01, 30d
    인터뷰 청소년·선생님          :v2, after v1, 45d
    결과 정리, H1~H3 판단         :milestone, m1, 2026-12-20, 0d

    section 1. 혜택 찾기 MVP
    혜택 수작업 수집, 스키마 확정 :b1, 2026-11-15, 40d
    맞춤 질문 + 추천 정적 웹      :b2, after m1, 30d
    센터에서 실제 사용 테스트     :b3, after b2, 30d
    크롤러 + Jev 자동화           :b4, after b3, 45d

    section 2. 수작업 매칭
    멘토 모집 5~10명              :c1, 2027-01-15, 30d
    선생님을 통한 연결 파일럿     :c2, after c1, 60d
    H4 판단                       :milestone, m2, after c2, 0d

    section 3. 매칭 기능
    로그인, DB, 멘토 프로필       :d1, after m2, 60d
    활동 기록 → 임팩트 리포트     :d2, after d1, 30d

    section 4. 수익
    기업 3곳에 제안, H5 판단      :e1, after m2, 60d
```

![일정](images/roadmap-timeline.svg)

## 3. MVP 범위와 이후 범위

크롤러보다 **손으로 모은 혜택 데이터로 추천 화면부터** 만든다. 인터뷰에서 바로 보여 주고 반응을 볼 수 있다.

```mermaid
flowchart LR
    subgraph MVP["1단계 MVP: 로그인 없음, 정적 사이트"]
        direction TB
        S1["수작업 혜택 목록<br/>data/processed/*.json"] --> S2["맞춤 질문<br/>나이·지역·학교 밖 여부·진로"]
        S2 --> S3["조건 매칭 추천"]
        S3 --> S4["진로 로드맵 + 단계별 혜택"]
        S4 --> S5["신청 체크리스트"]
    end

    subgraph LATER["이후 단계"]
        direction TB
        L1["공공 API + 민간 크롤러"] --> L2["Jev 조건 추출<br/>LLM 요약"]
        L2 --> L3["GitHub Actions<br/>매일 바뀐 공고만 처리"]
        L4["마감 알림"]
        L5["멘토 매칭<br/>로그인·DB"]
    end

    S5 -. "센터 테스트 후" .-> L1
    S5 -. "수요 확인 후" .-> L4
    S3 -. "H4 확인 후" .-> L5
```

![MVP 범위](images/roadmap-mvp-scope.svg)

## 원칙

1. **판단 기준을 미리 적는다.** 예: "인터뷰 15명 중 8명 이상이 '나중에 알고 놓쳤다'고 하면 H1 지지". 결과를 본 뒤 기준을 정하면 원하는 쪽으로 해석하게 된다.
2. **혜택 수집은 인터뷰와 동시에 시작한다.** 손으로 정리해 보면 Jev로 뽑을 필드(나이, 지역, 대상, 분야, 마감일)가 충분한지 알 수 있다.
3. **멘토 매칭은 스프레드시트와 카톡으로 시작한다.** H4 확인 전에 로그인과 DB를 만들면 버려질 가능성이 크다.
4. **크롤러는 가장 나중에 만든다.** 사람이 손으로 하기 힘들어졌을 때 만들어도 늦지 않다.
